spring.application.name=backend
server.port=${PORT:8080}

spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/jobboard}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:Narendra+2579}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

jwt.secret=${JWT_SECRET:ThisIsMySecretKeyForJobBoardApplicationPleaseChangeInProduction123456}